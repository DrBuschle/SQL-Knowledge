-- get all columns with a specified value

-- from one column

SELECT *
FROM Table_Name
WHERE Column_Name = Desired_Value;		-- Number can be written normally, text needs 'Text'

-- operators can also be others like >, <, <>, ...